# Global Hotkey

Jubako は `Win + Alt + V` で呼び出されます。

## ショートカットキーの競合
- 既に使われている可能性があるショートカットキーですが、本アプリではこれを優先的にフックします。
- 将来的には設定画面から変更可能にします。

## 呼び出し時の挙動
- UI がマウスカーソル位置、またはアクティブウィンドウの中央にポップアップ。
- 検索窓にフォーカスが当たっており、履歴や階層からアイテムを即座にフィルタリング可能。
